# Урок "Имена переменных"

## 7.1. Приведите пять примеров правильного именования булевых переменных в вашем коде в формате "было - стало".
1. status - is_active
2. check - is_valid
3. notFound - found
4. disabled - is_inabled
5. admin - is_admin

## 7.2. Найдите несколько подходящих случаев, когда в вашем коде можно использовать типичные имена булевых переменных.
- is_logged_in для проверки зарегистрирован ли пользователь;
- has_subsctiption для флага наличия подписки;
- found для флажка, если значение найдено в массиве;

## 7.3. Проверьте, правильно ли вы даёте имена индексам циклов. Попробуйте найти случай, когда вместо i j k нагляднее использовать более выразительное имя.
Проверил код курса по АСД: иногда использовал index вместо i.

Если обращение к индексу не требуется внутри цикла, использую наглядные имена, например
```python
for node in nodes:
    linkedList.add_in_tail(node)
```

Более выразительное имя уместно было использовать при проходе двумерного массива.
```python
row_index = 0

while row_index < rows:
    column_index = 0

    while column_index < columns:
        matrix[row_index][column_index] = 0
        column_index += 1

    row_index += 1
```

## 7.4. Попробуйте найти в своих решениях два-три случая, когда можно использовать пары имён - антонимы.
1. В курсе по АСД часто использовались head / tail.
2. На прошлой неделе для методов включения / отключения плавного скролла в браузере использовал lock / unlock.
3. Часто нужны previous / next.
4. В обёртках для работы с файловой системой уместны read / write.
5. Для управления поведением попапа функции open / close.
6. В контроллерах часто используются методы create / destroy.

## 7.5. Всем ли временным переменным в вашем коде присвоены выразительные имена? Найдите несколько случаев, когда временные переменные надо переименовать, и поищите, возможно, от некоторых временных переменных вам получится вообще полностью избавиться.
1. Чаще всего получается отказаться от булевых перменных, заменив их возвратом результата сравнения. Например, вместо:
```python
if user_role == ADMIN:
    has_access = True
else:
    has_access = False

return has_access
```

можно просто
return user_role == ADMIN

2. Лучше использовать более выразительные названия (small_list, big_list) в этом фрагменте кода.
```python
def intersection(self, set2: PowerSet) -> PowerSet:
        small = self
        big = set2
        if self.length > set2.length:
            small = set2
            big = self
        intersection = PowerSet()
        for bucket in small.buckets:
            for value in bucket:
                if big.get(value):
                    intersection.put(value)
        return intersection
```
