# Урок: имена классов

## Улучшите пять имён классов в вашем коде.
1. CustomerData - Customer
2. RequestHandler - AuthenticationHandler
3. ReportGenerator - SalesReport
4. OrderState - OrderStatus
5. UserEntity - User

## Улучшите семь имён методов и объектов по схеме из пункта 2.
1. user.active - user.isActive
2. validator.check(), policy.validate() - validator.validate(), policy.validate()
3. order.cancel(), booking.abort() - order.abort(), booking.abort()
4. onMouseLeave(), MouseEnterHandler() - onMouseLeave(), onMouseEnter()
5. invoice.create(), report.generate() - invoice.create(), report.generate()
6. job.execute(), task.run() - job.run(), task.run()
7. DateHelper - BusinessCalendar
